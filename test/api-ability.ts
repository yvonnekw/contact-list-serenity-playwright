import { Ability, UsesAbilities } from '@serenity-js/core';
import { AxiosInstance } from 'axios';
/*
export class /*CallAnApi implements Ability {
    static using(axiosInstance: AxiosInstance) {
        return new CallAnApi(axiosInstance);
    }

    constructor(private axiosInstance: AxiosInstance) { }

    async sendRequest(requestConfig: any) {
        return this.axiosInstance(requestConfig);
    }
}

export interface WithApiAbility extends UsesAbilities {
    using: (ability: CallAnApi) => CallAnApi;
}
*/